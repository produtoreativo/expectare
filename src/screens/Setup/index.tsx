import * as React from 'react';
import {Text, View} from 'react-native';

function Setup(): React.JSX.Element {
  return (
    <View>
      <Text> Colocar os Inputs do form</Text>
    </View>
  );
}

export default Setup;
